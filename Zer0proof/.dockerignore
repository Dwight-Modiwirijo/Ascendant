.git
.lake
# dist is deliberately NOT ignored: the image compares the published .olean
# files against the ones it builds, so the artifact under test is the one that
# was actually shipped, not a fresh rebuild of it.
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.log
*.tmp
__pycache__
*.pyc
