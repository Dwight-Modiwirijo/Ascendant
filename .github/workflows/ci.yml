name: CI
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: Zer0proof

    steps:
      - uses: actions/checkout@v4

      - name: Install elan (Lean toolchain)
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain=none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Ensure toolchain from lean-toolchain
        run: |
          TOOLCHAIN="$(cat lean-toolchain)"
          ~/.elan/bin/elan toolchain install "$TOOLCHAIN"
          ~/.elan/bin/elan override set "$TOOLCHAIN"
          ~/.elan/bin/lean --version
          ~/.elan/bin/lake --version || true

      # Exit 2 means: the public distribution was built and verified, and the
      # strong certificate assembly is absent. That is a known, reportable
      # state, not a build failure. Any other non-zero code fails the job.
      - name: Run CI script
        id: ci
        run: |
          set +e
          bash scripts/ci.sh
          rc=$?
          set -e
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          if [ "$rc" -eq 0 ]; then
            echo "::notice::Public distribution built. Strong certificate included."
          elif [ "$rc" -eq 2 ]; then
            echo "::warning::Public distribution built. Strong certificate assembly absent."
          else
            echo "::error::CI failed with exit code $rc"
            exit "$rc"
          fi

      - name: Publish the distribution
        if: always() && hashFiles('Zer0proof/dist/SHA256SUMS') != ''
        uses: actions/upload-artifact@v4
        with:
          name: zer0proof-dist
          path: Zer0proof/dist
          if-no-files-found: error

      - name: Independent re-verification of the manifest
        if: always() && hashFiles('Zer0proof/dist/SHA256SUMS') != ''
        run: |
          cd dist
          sha256sum -c SHA256SUMS
          cat SCOPE.txt
